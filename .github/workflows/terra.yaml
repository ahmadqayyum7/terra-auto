name: Terraform EC2 CI/CD

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  terraform:
    name: Provision EC2 with Terraform
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.6.0

      - name: Terraform Init
        run: terraform init

      - name: Refresh Terraform State
        run: terraform refresh

      - name: Check Terraform State (Ensure Resources Are Correct)
        run: terraform state list

      - name: Import Resources if Missing
        run: |
          terraform import aws_security_group.web_sg sg-xxxxxxxxxx || echo "Security Group already imported"
          terraform import aws_key_pair.key terra-key || echo "Key Pair already imported"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply (on main only)
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
